// hooks/useGSAPAnimations.js
// Purpose: Custom React hook encapsulating reusable GSAP animation patterns.
// Exports functions: fadeInUp(target, options), staggerFadeIn(targets, options), pinSection(trigger, options).
// Handles GSAP context creation and cleanup on unmount.
