'use client';
// context/ThemeContext.jsx
// Purpose: React Context for application-wide theme management.
// Provides: theme (light | dark), toggleTheme(), systemPreference detection.
// Persists theme preference to localStorage; syncs with system prefers-color-scheme.
