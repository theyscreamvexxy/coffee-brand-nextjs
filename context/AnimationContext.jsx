'use client';
// context/AnimationContext.jsx
// Purpose: React Context for global animation state management.
// Provides: animationsEnabled (boolean), reducedMotion (respects prefers-reduced-motion media query), setAnimationsEnabled().
// Used to conditionally enable/disable GSAP and Framer Motion animations site-wide.
