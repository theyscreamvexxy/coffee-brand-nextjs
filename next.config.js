// next.config.js
// Purpose: Next.js configuration file.
// Configures: image domains (Cloudinary, Instagram), experimental features, webpack aliases,
// environment variable exposure, and any custom headers/redirects/rewrites.

/** @type {import('next').NextConfig} */
const nextConfig = {
  // Configuration will be added here during implementation.
};

module.exports = nextConfig;
