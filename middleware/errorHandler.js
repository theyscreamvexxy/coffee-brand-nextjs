// middleware/errorHandler.js
// Purpose: Global Express.js error-handling middleware.
// Catches errors propagated via next(err); returns structured JSON error responses.
// Differentiates between validation errors, not-found errors, and generic server errors.
